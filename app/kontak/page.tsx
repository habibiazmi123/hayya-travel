import type { Metadata } from "next";
import { SectionHeading } from "@/components/SectionHeading";
import { SITE } from "@/lib/site";
import { generalWaLink } from "@/lib/whatsapp";

export const metadata: Metadata = { title: "Kontak Hayya Tour & Travel", description: "Hubungi Hayya Tour & Travel Bandung melalui WhatsApp, telepon, email, atau kunjungi alamat kami." };

export default function KontakPage() { return <div className="mx-auto max-w-3xl px-4 py-16"><SectionHeading eyebrow="KONTAK" title="Mari Terhubung" description="Tim Hayya siap membantu rencana perjalanan ibadah Anda." /><div className="mt-10 rounded-3xl border border-[#eee8dc] bg-white p-7"><div className="space-y-4 text-sm leading-relaxed text-sage"><p><strong className="text-pine">Alamat</strong><br />{SITE.address}</p><p><strong className="text-pine">WhatsApp / Telepon</strong><br /><a className="underline" href={`tel:${SITE.phoneIntl}`}>{SITE.phoneDisplay}</a> <span aria-hidden="true">•</span> {SITE.secondaryPhoneDisplay}</p><p><strong className="text-pine">Email</strong><br /><a className="underline" href={`mailto:${SITE.email}`}>{SITE.email}</a></p><p><strong className="text-pine">Media sosial</strong><br />{SITE.socials.map((social) => <a key={social.label} href={social.href} target="_blank" rel="noopener noreferrer" className="mr-3 underline">{social.label}</a>)}</p></div><div className="mt-7 flex flex-wrap gap-3"><a href={generalWaLink()} target="_blank" rel="noopener noreferrer" className="inline-flex min-h-12 items-center rounded-full bg-gold px-7 font-bold text-pine">Chat WhatsApp</a><a href={SITE.mapsUrl} target="_blank" rel="noopener noreferrer" className="inline-flex min-h-12 items-center rounded-full border border-pine px-7 font-bold text-pine">Buka di Maps</a></div></div></div>; }
